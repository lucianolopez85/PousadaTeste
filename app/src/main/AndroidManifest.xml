<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pousadateste">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PousadaTeste">
        <activity android:name=".EventoSocial"></activity>
        <activity android:name=".Lazer" />
        <activity android:name=".FormPagamento" />
        <activity android:name=".FormReserva" />
        <activity android:name=".QuartoStandard" />
        <activity android:name=".CategoriaAcomodacoes" />
        <activity android:name=".MenuPrincipal" />
        <activity android:name=".LayoutEntrada">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>